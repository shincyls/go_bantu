<% @projects.each do |d| %>
    <div class="col-sm-2 col-md-3">
        <div class="card card-lift--hover shadow mb-3", id="dress-#{d.id}">
            <div class="card-body">

           
            </div>
        </div>
    </div>
<% end %>




